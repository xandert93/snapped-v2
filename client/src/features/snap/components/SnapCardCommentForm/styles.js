import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  'snap-comment-form': {
    paddingBottom: 0,
  },

  'snap-comment-input': {
    fontSize: 16,
  },

  'pen-icon-button': {
    paddingBottom: 5,
    paddingLeft: 5,
  },
}));
