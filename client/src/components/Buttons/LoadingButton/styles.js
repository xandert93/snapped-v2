import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  'loading-button': {
    position: 'relative',
  },

  'circular-progress-container': {
    position: 'absolute',
    display: 'flex',
  },
}));
